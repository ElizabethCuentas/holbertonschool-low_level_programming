#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char l;
for (l = 'a' ; l <= 'z' ; l++)
putchar (l);
putchar ('\n');
for (l = 'A' ; l <= 'Z' ; l++)
return (0);
}
